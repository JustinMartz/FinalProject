import { BehaviorReportRemark } from './behavior-report-remark';

describe('BehaviorReportRemark', () => {
  it('should create an instance', () => {
    expect(new BehaviorReportRemark()).toBeTruthy();
  });
});
